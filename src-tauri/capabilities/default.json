{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions + FS & Dialog",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default",
    "process:default",
    "fs:write-files",
    "fs:allow-rename",
    {
      "identifier": "fs:scope",
      "allow": [{ "path": "$HOME/**/*" }]
    }
  ]
}
